//Multiple Statement in one line without {}
 if(!q.empty() && q.top() < x) ans += (x - q.top()),q.top(), q.pop(), q.push(x);

 //New way
 int days = prices.length, buy[] = new int[days], sell[] = new int[days];



 //Two new Log in C of loge and log2
 log(1)
 log2(3)